/**
 * Бизнес-правила для игры «Колесо фортуны».
 */
export const FORTUNE_BUSINESS_RULES = {
  /** Максимальное количество попыток в день для одного пользователя */
  maxDailyAttempts: 3,
  /** Лимит количества хранимых записей в истории попыток */
  historyLimit: 10,
  /** Точное время анимации вращения колеса в миллисекундах (4 секунды по ТЗ) */
  spinDuration: 4000,
  /** Задержка в миллисекундах перед отображением модального окна после остановки колеса */
  resultModalDelay: 500,
} as const;

/**
 * Технические параметры колеса фортуны (отрисовка, тайминги, углы).
 */
export const FORTUNE_TECHNICAL_CONFIG = {
  /** Параметры отображения и анимации колеса */
  wheel: {
    /** Координата X центра колеса в пространстве SVG */
    cx: 195,
    /** Координата Y центра колеса в пространстве SVG */
    cy: 195,
    /** Внешний радиус колеса */
    r: 183,
    /** Внутренний радиус (радиус центрального хаба) */
    ir: 24,
    /** Количество секторов на колесе */
    numSectors: 12,
    /** Минимальное количество полных оборотов колеса при спине */
    extraTurnsMin: 5,
    /** Случайное дополнительное количество оборотов при спине */
    extraTurnsVar: 5,
  },
  /** Задержка для искусственного замедления загрузки призов (лоадер/плейсхолдер) в мс */
  prizesLoadDelay: 1500,
} as const;
