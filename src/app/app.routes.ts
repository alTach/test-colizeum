import { Routes } from '@angular/router';
import { FortuneGameComponent } from './pages/fortune-game/fortune-game.component';

export const routes: Routes = [
  {
    path: '',
    component: FortuneGameComponent,
  },
];
